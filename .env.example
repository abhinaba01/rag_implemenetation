# Copy to .env and fill in. .env is gitignored and must never be committed.

# Used by pipeline/llm.py for generation, grading and query decomposition.
OPENAI_API_KEY=sk-...

# Only needed if you re-run generate_questions.py (gold question drafting).
GOOGLE_API_KEY=...
